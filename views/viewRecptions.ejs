<!DOCTYPE html>
<html>
<head>
  <title>Receptionists List</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f8fc;
      margin: 0;
      padding: 0;
    }

    .content {
      margin-left: 250px;
      padding: 30px;
      transition: margin-left 0.3s;
    }

    h1 {
      text-align: center;
      color: #004080;
      margin-bottom: 30px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background-color: white;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    table th {
      text-align: center !important;
      vertical-align: middle;
    }

    th, td {
      padding: 12px;
      text-align: center;
      border: 1px solid #ccc;
    }

    th {
      background-color: #007bff;
      color: white;
    }

    tr:nth-child(even) {
      background-color: #f9f9f9;
    }

    .btn-back {
      display: inline-block;
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #004080;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .btn-back:hover {
      background-color: #0059b3;
    }

    .btn-action {
      padding: 6px 12px;
      border: none;
      border-radius: 4px;
      color: white;
      cursor: pointer;
      text-decoration: none;
    }

    .btn-edit {
      background-color: #28a745;
    }

    .btn-edit:hover {
      background-color: #218838;
    }

    .btn-delete {
      background-color: #dc3545;
    }

    .btn-delete:hover {
      background-color: #c82333;
    }
  </style>
</head>
<body>

  <%- include("adminpage.ejs") %>

  <div class="content">
    <h1>All Receptionists</h1>

    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Contact</th>
          <th>Update</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        <% receptions.forEach(recep => { %>
          <tr>
            <td><%= recep.name %></td>
            <td><%= recep.contact %></td>
            <td>
              <a href="/editreception/<%= recep.id %>" class="btn-action btn-edit">Update</a>
            </td>
            <td>
              <a href="/deletereception/<%= recep.receptionId%>" class="btn-action btn-delete" onclick="return confirm('Are you sure you want to delete this receptionist?');">Delete</a>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <a href="/admindashboard" class="btn-back">Back to Admin Page</a>
  </div>

</body>
</html>
